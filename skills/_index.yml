generated: '2026-08-12'
method: generated
source: openapi/finditparts-reseller-api-openapi.yml
note: >-
  Packaged Agent Skills for the FinditParts Reseller API, generated by API Evangelist.
  FinditParts publishes no AGENTS.md and no skill files of its own — a search of the
  developer page, the API documentation and the GitHub org found none. Every operationId
  referenced below was verified to exist verbatim in
  openapi/finditparts-reseller-api-openapi.yml; every rule about authentication,
  errno semantics, idempotency and pagination is transcribed from the provider's own
  published documentation, not invented.
skill_count: 4
skills:
- file: finditparts-find-a-part.md
  name: Find a heavy-duty part on FinditParts
  description: Search the catalog by part number, cross-reference or free text, then
    resolve the exact purchasable variant with its price, stock and ship cutoff.
  api: openapi/finditparts-reseller-api-openapi.yml
  operations: [lookupProductByPartNumber, productSearch, getProduct, getProductsMulti, variantLookup]
  read_only: true
- file: finditparts-quote-shipping.md
  name: Quote shipping for a FinditParts order
  description: Get carrier options, cost and delivery estimates for a proposed set of
    line items and a destination address, on either the reseller or the partner surface.
  api: openapi/finditparts-reseller-api-openapi.yml
  operations: [shippingMethods, partnersShippingMethods, getCartShippingMethods, variantLookup]
  read_only: true
- file: finditparts-onboard-a-reseller-customer.md
  name: Onboard and order for a reseller customer on FinditParts
  description: Link one of your own customers to a FinditParts account with a hosted
    USER_SETUP session, then place an order with a hosted NEW_ORDER session and confirm
    the result server-side.
  api: openapi/finditparts-reseller-api-openapi.yml
  operations: [createResellerCustomer, listResellerCustomers, createResellerCustomerSession,
    getResellerCustomerSession, cancelResellerCustomerSession, shippingMethods]
  read_only: false
  human_in_the_loop: required
- file: finditparts-cart-checkout.md
  name: Build a cart and check out on FinditParts
  description: Drive the direct API-key path — exchange user credentials for a session
    JWT, build a cart, set addresses and shipping, then complete the order and reconcile it.
  api: openapi/finditparts-reseller-api-openapi.yml
  operations: [createSession, refreshSession, createCart, addCartLineItem, changeCartLineItems,
    setCartShippingAddress, getCartShippingMethods, selectCartShippingMethod, setCartPoNumber,
    completeCartWithCreditCard, completeCartWithCorporateBilling, getCart, searchOrders, getOrder]
  read_only: false
  irreversible: true
cross_cutting_rules:
- Every request needs a freshly signed short-lived HS256 JWT (iss + exp), or an issued
  api- key; user-specific work needs a user session JWT obtained via createSession.
- errno appears on 200 responses. HTTP status alone is not a success signal.
- Errors are returned as HTTP 500 with a {errno, message} envelope, not RFC 9457.
- There is no idempotency key anywhere on this API. Never blindly retry a write;
  reconcile with searchOrders/getOrder instead.
- No rate limits are published and no Retry-After is returned. Serialize and back off manually.
- Product pricing depends on the credential — account_price only appears when the JWT
  carries a sub Customer Reference.
